applicationType: default
defaultPackage: com.graqr
testFramework: spock
sourceLanguage: kotlin
buildTool: gradle_kotlin
features: [app-name, gradle, http-client, jdbc-hikari, kotlin, kotlin-application, ksp, log4j2, micronaut-aot, micronaut-build, micronaut-http-validation, netty-server, postgres, readme, serialization-jackson, shade, spock, static-resources, test-resources, yaml, yaml-build]
